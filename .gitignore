# Node 相關（跑 tools/check-kk.mjs 或 .bat 時本機才會產生，不需推上 GitHub）
node_modules/
package.json
package-lock.json
npm-debug.log*

# 待刪除的舊檔（已改用 TTS 合成，不需推上 GitHub）
delete/

# 作業系統雜項
Thumbs.db
.DS_Store

# 交接文件：刻意不入庫，僅隨資料夾整包（USB／備份）複製傳遞（不推 GitHub）
# ⚠️ 本檔目前仍被追蹤，要真正停止推送需執行一次：git rm --cached SKILL.md
SKILL.md
